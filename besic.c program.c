#include<stdio.h>
   main()
   {
 int price,produce,total_of_produce;
 printf("Enter the price");
 scanf("%d",&price);
 printf("Enter number of produce");
 scanf("%d",&produce);
 total_of_produce=price*produce;
 printf("the total of the total of produce =%d",total_of_produce);
   }
